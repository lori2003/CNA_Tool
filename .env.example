# ── Toolbox CNA — Variabili d'ambiente ──
# Copia questo file come .env e inserisci i valori reali.

# API Key per il provider AI (es. OpenRouter)
OPENROUTER_API_KEY=

# Porta del server (default: 8501)
PORT=8501
